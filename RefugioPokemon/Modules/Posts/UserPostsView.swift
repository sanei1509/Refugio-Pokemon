import SwiftUI

struct UserPostsView: View {
    
    var body: some View {
        VStack{
            Text("Listado de publicaciones").font(.title)
        }
        .navigationTitle("Publicaciones de usuarios")
    }
}
